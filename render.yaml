services:
  - type: web
    name: smarthire-backend
    env: node
    buildCommand: pnpm install && pnpm build --filter=server
    startCommand: cd apps/server && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
